import styled from 'styled-components';
import { colors } from '../style';

export const ButtonStyle = styled.button`
  width: 100%;
  padding: 4px 0;
  font-size: 14px;
  font-weight: bold;
  color: ${colors.pink};
  display: block;
  text-align: center;
  border: none;
  cursor: pointer;
`;
